<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="description" content="{{ description or site.description }}" />
  <title>{{ title }} — Axiom Newsletter</title>

  <!-- Shared design tokens -->
  <link rel="stylesheet" href="/newsletter/css/design-tokens.css" />

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet" />

  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      background-color: var(--color-cream);
      color: var(--color-ink);
      font-family: var(--font-body);
      -webkit-font-smoothing: antialiased;
      line-height: 1.6;
    }

    /* Nav */
    .nav {
      position: sticky;
      top: 0;
      z-index: 50;
      background: rgba(248, 244, 236, 0.95);
      backdrop-filter: blur(4px);
      border-bottom: 0.5px solid rgba(201, 164, 76, 0.3);
      padding: 1rem 1.5rem;
    }
    .nav-inner {
      max-width: 64rem;
      margin: 0 auto;
      display: flex;
      align-items: center;
      justify-content: space-between;
    }
    .nav-brand {
      font-family: var(--font-heading);
      font-size: 1.35rem;
      font-weight: 300;
      letter-spacing: 0.15em;
      color: var(--color-green);
      text-decoration: none;
    }
    .nav-links {
      display: flex;
      gap: 2rem;
      list-style: none;
    }
    .nav-links a {
      font-family: var(--font-body);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      color: var(--color-green);
      text-decoration: none;
      transition: color 200ms ease;
    }
    .nav-links a:hover,
    .nav-links a.active {
      color: var(--color-terracotta);
    }

    /* Main content */
    .main {
      max-width: 48rem;
      margin: 0 auto;
      padding: 3rem 1.5rem 6rem;
    }

    /* Page header */
    .page-header {
      text-align: center;
      padding: 4rem 0 3rem;
      border-bottom: 0.5px solid rgba(201, 164, 76, 0.3);
      margin-bottom: 3rem;
    }
    .page-header .eyebrow {
      font-size: 0.7rem;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: var(--color-gold);
      margin-bottom: 1rem;
    }
    .page-header h1 {
      font-family: var(--font-heading);
      font-size: clamp(2rem, 5vw, 3.5rem);
      font-weight: 300;
      color: var(--color-green);
    }
    .page-header p {
      color: rgba(26, 26, 24, 0.6);
      margin-top: 1rem;
      font-size: 0.95rem;
    }

    /* Post list */
    .post-list {
      display: flex;
      flex-direction: column;
      gap: 0;
    }
    .post-item {
      padding: 2rem 0;
      border-bottom: 0.5px solid rgba(201, 164, 76, 0.2);
    }
    .post-item:last-child { border-bottom: none; }
    .post-date {
      font-size: 0.7rem;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: var(--color-gold);
      margin-bottom: 0.5rem;
    }
    .post-title a {
      font-family: var(--font-heading);
      font-size: clamp(1.3rem, 2.5vw, 1.7rem);
      font-weight: 400;
      color: var(--color-green);
      text-decoration: none;
      transition: color 200ms ease;
    }
    .post-title a:hover { color: var(--color-terracotta); }
    .post-author {
      font-size: 0.8rem;
      color: rgba(26, 26, 24, 0.5);
      margin-top: 0.25rem;
    }
    .post-excerpt {
      margin-top: 0.75rem;
      font-size: 0.9rem;
      color: rgba(26, 26, 24, 0.65);
      line-height: 1.7;
    }
    .post-read-more {
      display: inline-block;
      margin-top: 1rem;
      font-size: 0.8rem;
      color: var(--color-terracotta);
      text-decoration: none;
      transition: color 200ms ease;
    }
    .post-read-more:hover { color: var(--color-green); }

    /* Article */
    .article-header {
      text-align: center;
      padding: 4rem 0 3rem;
      border-bottom: 0.5px solid rgba(201, 164, 76, 0.3);
      margin-bottom: 3rem;
    }
    .article-header h1 {
      font-family: var(--font-heading);
      font-size: clamp(1.8rem, 5vw, 3rem);
      font-weight: 300;
      color: var(--color-green);
      line-height: 1.2;
      margin-bottom: 1rem;
    }
    .article-meta {
      font-size: 0.8rem;
      color: rgba(26, 26, 24, 0.5);
    }
    .article-meta span { color: var(--color-gold); }

    /* Article body */
    .article-body { line-height: 1.8; }
    .article-body h2 {
      font-family: var(--font-heading);
      font-size: 1.5rem;
      font-weight: 400;
      color: var(--color-green);
      margin: 2.5rem 0 1rem;
    }
    .article-body h3 {
      font-family: var(--font-heading);
      font-size: 1.2rem;
      font-weight: 400;
      color: var(--color-green);
      margin: 2rem 0 0.75rem;
    }
    .article-body p { margin-bottom: 1.25rem; color: rgba(26, 26, 24, 0.8); font-size: 1rem; }
    .article-body blockquote {
      border-left: 4px solid var(--color-terracotta);
      padding: 0.5rem 0 0.5rem 1.5rem;
      margin: 2rem 0;
      font-family: var(--font-heading);
      font-style: italic;
      font-size: 1.1rem;
      color: var(--color-green);
    }
    .article-body ol, .article-body ul {
      margin: 1rem 0 1.25rem 1.5rem;
      color: rgba(26, 26, 24, 0.8);
    }
    .article-body li { margin-bottom: 0.4rem; }
    .article-body em { font-style: italic; }
    .article-body strong { font-weight: 600; }
    .article-body a { color: var(--color-terracotta); text-decoration: underline; }
    .article-body hr {
      border: none;
      border-top: 0.5px solid var(--color-gold);
      margin: 2.5rem 0;
    }

    /* Back link */
    .back-link {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      font-size: 0.8rem;
      color: rgba(26, 26, 24, 0.5);
      text-decoration: none;
      transition: color 200ms ease;
      margin-bottom: 2rem;
    }
    .back-link:hover { color: var(--color-terracotta); }

    /* Footer */
    .footer {
      background: var(--color-green);
      color: rgba(248, 244, 236, 0.7);
      text-align: center;
      padding: 2.5rem 1.5rem;
      font-size: 0.8rem;
    }
    .footer a {
      color: var(--color-gold);
      text-decoration: none;
    }

    @media (max-width: 640px) {
      .nav-links { gap: 1rem; }
      .nav-links a { font-size: 0.65rem; }
    }
  </style>
</head>
<body>
  <!-- Nav — links back to React hash routes -->
  <nav class="nav">
    <div class="nav-inner">
      <a href="{{ site.reactBaseUrl }}" class="nav-brand">AXIOM</a>
      <ul class="nav-links">
        {% for link in site.navLinks %}
          <li>
            <a href="{{ link.href }}"{% if link.active %} class="active"{% endif %}>
              {{ link.label }}
            </a>
          </li>
        {% endfor %}
      </ul>
    </div>
  </nav>

  <main>
    {{ content | safe }}
  </main>

  <footer class="footer">
    <p>© {{ site.title }} · <a href="{{ site.reactBaseUrl }}">axiom.nsut</a></p>
  </footer>
</body>
</html>
